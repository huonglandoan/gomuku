## 🛠 Requirements (Yêu cầu hệ thống)

Dự án yêu cầu **Python 3.10+**. Nên sử dụng môi trường ảo (`venv`) để quản lý các thư viện.

### 1. Khởi tạo môi trường ảo (Khuyên dùng)
Bạn có thể khởi tạo và kích hoạt môi trường ảo bằng các lệnh sau:

- **Trên Windows:**
  ```bash
  python -m venv venv
  .\venv\Scripts\activate

- **Trên MacOS/Linux:**
  python3 -m venv venv
  source venv/bin/activate

### 2. Cài đặt thư viện
    pip install pygame numpy

### 3. Hướng dẫn chạy
    python main.py